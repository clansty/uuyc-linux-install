# 工作约定

- 用中文交流；执行安装先完整阅读 INSTALL.md。
- INSTALL.md 面向没有项目背景的安装 agent，保持步骤独立、可执行，不依赖聊天历史。
- 不把本机路径、账号、设备名、LUID、日志和截图写入可复用工具。
- 补丁必须校验版本和原始 SHA-256。失败时停止，不能跳过校验。
- 高画质补丁是默认安装步骤，已验证 GPU 不是白名单。应用后失败时继续诊断并验证其他可用解码路径，不因型号不同而默认跳过，也不能把其他格式宣称为当前补丁已支持。
- 对现有安装做任何修改前先确认目标 prefix、备份文件和注册表。
- 只停止明确属于目标 prefix 的进程，保留其他 Wine 会话。
- 用户登录、验证码及远端设备授权由用户完成。
- 开始 QA 前阅读 AGENTS_QA.md；记录解决的 QA 障碍，区分实际使用验证和静态检测。
